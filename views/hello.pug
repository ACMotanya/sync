extends layout.pug

block content
  if name
    h2 Welcome, #{name}!
  else
    form.bd-example(action='/hello', method='post')
      label(for='input') Enter your name:
        input#input(type='text', placeholder='Example input')
      button(type='submit') Submit

